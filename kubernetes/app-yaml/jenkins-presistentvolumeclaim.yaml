kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jenkins-home
  namespace: kube-system
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
  storageClassName: jenkins-home
